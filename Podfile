# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FallDetectionLib' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftyBeaver'
  pod 'RxSwift', '~> 4.0'

  # Pods for FallDetectionLib

  target 'FallDetectionLibTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Quick'
    pod 'Nimble'
  end

end
